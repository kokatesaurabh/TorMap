import React from 'react';

const Dashboard = () => {
  const pageStyle = {
    padding: '20px',
    backgroundColor: '#ecf0f1',
    borderRadius: '5px',
  };

  return (
    <div style={pageStyle}>
      <h2>Dashboard</h2>
      <p>Welcome to the TorMap Dashboard.</p>
    </div>
  );
};

export default Dashboard;
